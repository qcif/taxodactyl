# Test configuration with custom values
blast_max_target_seqs: 5000
bold_database: "TEST_DATABASE"

inputs:
  fasta_max_sequences: 200
  facility_name: "Test Facility"

criteria:
  alignment_min_identity: 0.99
  
report:
  title: "Test Report Title"
  debug: true