/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    qcif/taxodactyl Nextflow config file
    - This is the main configuration file for the pipeline.
    - It includes all other config fragments required for pipeline execution.
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

// Include pipeline parameter definitions (input/output, defaults, etc.)
includeConfig 'conf/params.config'

// Include process-specific settings (resources, container, etc.)
includeConfig 'conf/process.config'

// Include pipeline manifest (metadata about the pipeline itself)
includeConfig 'conf/manifest.config'

// Include parameter validation rules and schema
includeConfig 'conf/validation.config'

// Include execution profiles (e.g., singularity, docker, local, etc.)
includeConfig 'conf/profiles.config'

// Include miscellaneous settings (logging, custom tweaks, etc.)
includeConfig 'conf/misc.config'